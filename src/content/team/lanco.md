---
# One file per member. `photo` is a file under public/team/; without it the card
# falls back to a silhouette. `number` is the shirt and the roster order,
# `position` is the slot on the pitch (gk, def, mid, att) — no position means
# the bench. The body below the frontmatter is the bio, and it is optional.
name: 'Lanco'
nickname: ''
role: ''
photo: 'lanco.jpg'
number: 9
position: 'mid'
---

